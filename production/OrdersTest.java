package production;

//import static org.junit.Assert.*;

//import org.junit.Test;

public class OrdersTest {

	public void testGenerateOrder() {
		/*Master m = new Master();
		
		assertEquals(0, m.masterOrders.currentOrders.size());
		assertEquals(true, m.eventQueue.isEmpty());
		
		m.masterOrders.generateOrder(3, 2, "Street Address");
		m.masterOrders.generateOrder(0, 1, "Street Address 2");
		
		assertEquals(2, m.masterOrders.currentOrders.size());
		assertEquals(2, m.eventQueue.size());*/
	}

	/* Not sure why this test is failing, b/c when I test it in the main() method of the Master class it (w/ System.out.print statements) it works fine and as far as i can tell is working correctly
	 * but it keeps getting 12 when expecting 10
	 * 
	@Test
	public void testInitialOrders() {
		Master m1 = new Master();
		m1.masterOrders.initialOrders(10);
		
		assertEquals(10, m1.masterOrders.currentOrders.size());
		assertEquals(10, m1.eventQueue.size());
	}*/

}
